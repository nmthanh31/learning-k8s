# PodDisruptionBudget đảm bảo luôn có ít nhất 1 Pod Grafana hoạt động khi drain node
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: grafana-pdb
  namespace: monitoring
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: grafana
